# contract

Contract DSL, Standard Schema types, and inference helpers. Zero runtime dependencies.

Status: M0 scaffold. Implementation begins in M1; there is no public API yet.
See [the implementation plan](../../docs/PLAN.md). This package remains private
until its documented API, behavioral and type tests, and release metadata are ready.
